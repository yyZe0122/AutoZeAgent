# AutoZeAgent v0.1.0

**Alpha · first binary Pre-release**  
**Alpha · 首个二进制预发布**

Local-first, policy-controlled automation agent in Go: dual-track session chat, Tool Broker, and a single SQLite source of truth.

本地优先、受策略约束的 Go 自动化智能体：双轨会话对话、Tool Broker、单一 SQLite 事实源。

> Review config, policy, and permission boundaries before using important data or privileged credentials.  
> 在接触重要数据或高权限凭据前，请先检查配置与权限边界。

## Highlights / 亮点

- **Dual-track chat:** **agent** (build · read/write workspace tools) and **plan** (read-only analysis); no interactive Planner approval pipeline.
- **双轨会话：** **agent**（可写）与 **plan**（只读分析）；无独立 Planner 整单审批轨。
- **Tool Broker** is the only model-requested effect path (policy → grant → containment → limits → audit). Optional tool-call permission (`ask`: once / similar / permanent / deny).
- **Tool Broker** 为模型副作用唯一通道。可选 tool-call 权限（`ask`：once / similar / permanent / deny）。
- **In-process memory**, context packing/compaction, chat-native scheduled jobs (`/cron`), MCP via Broker.
- **进程内记忆**、上下文装配/压缩、chat 原生定时任务（`/cron`）、经 Broker 的 MCP。
- **TUI primary UX** (`aze` / no-arg); CLI secondary. Structured logs (`aze logs`).
- **TUI 为主路径**（`aze`）；CLI 次要。结构化日志（`aze logs`）。

## Package layout / 包内容

Each platform archive contains **three binaries** plus docs and packaging helpers:

每个平台归档内含 **三个二进制** 以及文档与安装辅助文件：

| Path | Role |
| --- | --- |
| `autozeagent` / `autozeagent.exe` | CLI + TUI |
| `aze` / `aze.exe` | Short TUI launcher (same CLI) |
| `autozeagentd` / `autozeagentd.exe` | Long-running daemon |
| `README.md`, `LICENSE` | Project docs / license |
| `configs/` | Example config + JSON schema |
| `packaging/scripts/` | Install / upgrade / backup helpers |
| `packaging/systemd/autozeagent.service` | Linux system unit |

## Assets / 发布资产

File names include the **version** (no leading `v`). Checksums file has a fixed name.

文件名包含**版本号**（无前导 `v`）。校验文件名固定。

| Platform / 平台 | Asset / 文件 |
| --- | --- |
| Linux amd64 | `autozeagent_0.1.0_linux_amd64.tar.gz` |
| Linux arm64 | `autozeagent_0.1.0_linux_arm64.tar.gz` |
| macOS amd64 | `autozeagent_0.1.0_darwin_amd64.tar.gz` |
| macOS arm64 | `autozeagent_0.1.0_darwin_arm64.tar.gz` |
| Windows amd64 | `autozeagent_0.1.0_windows_amd64.zip` |
| Windows arm64 | `autozeagent_0.1.0_windows_arm64.zip` |
| Checksums | `checksums.txt` (SHA-256) |

Download URL shape:

```text
https://github.com/yyZe0122/AutoZeAgent/releases/download/v0.1.0/<asset>
```

## Verify / 校验

```bash
# after downloading checksums.txt and the archive into the same directory
sha256sum -c checksums.txt
# macOS: shasum -a 256 -c checksums.txt
```

## Install / 安装

**Pin this release** (recommended while tagged Pre-release; GitHub `latest` may skip pre-releases):

**建议固定版本**（Pre-release 时 `latest` 可能不指向本 tag）：

```bash
export AUTOZEAGENT_VERSION=v0.1.0
curl -fsSL "https://raw.githubusercontent.com/yyZe0122/AutoZeAgent/main/packaging/scripts/install-user.sh" | sh
```

```powershell
$env:AUTOZEAGENT_VERSION = 'v0.1.0'
irm "https://raw.githubusercontent.com/yyZe0122/AutoZeAgent/main/packaging/scripts/install.ps1" | iex
```

Or extract the archive and place the three binaries on `PATH`.  
或解压归档后将三个二进制放入 `PATH`。

```bash
autozeagent version    # expect 0.1.0
autozeagentd --check
aze                    # TUI
```

Config lives only under the OS config directory (e.g. `~/.config/autozeagent/`). Prefer `{env:VAR}` for API keys. See README.

配置仅放在系统/用户配置目录；API Key 请用 `{env:…}`。详见 README。

## Notes / 说明

- Production shape: `autozeagentd` + CLI/TUI + `core.db` only.
- 生产形态仅三件套：守护进程 + CLI/TUI + 单库。
- Design records: `docs/architecture/`; living backlog: `docs/optimization/current.md`.
- 设计见 `docs/architecture/`；活 backlog 见 `docs/optimization/current.md`。
- Report vulnerabilities via `SECURITY.md`.
- 安全问题见 `SECURITY.md`。
